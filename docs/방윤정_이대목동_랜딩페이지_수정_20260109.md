# 방윤정 이대목동 랜딩페이지 수정 작업 로그

**작업일**: 2026-01-09

---

## 1. 헤더 로고 변경

**파일**: `components/main/Header.tsx` (54줄)

| 변경 전 | 변경 후 |
|---------|---------|
| Font Awesome 아이콘 (`fa-solid fa-hospital`) | 이미지 (`/logo_ehwa.svg`) |

```tsx
// 변경 전
<i className="fa-solid fa-hospital" aria-hidden="true"></i>

// 변경 후
<img src="/logo_ehwa.svg" alt="이대목동병원 로고" className="h-8 w-auto" />
```

---

## 2. 사전문진표 버튼 아이콘 변경

**파일**: `components/main/Header.tsx` (108줄, 182줄)

| 변경 전 | 변경 후 |
|---------|---------|
| `fa-regular fa-file-lines` (문서 아이콘) | `fa-solid fa-file-arrow-down` (다운로드 아이콘) |

- PC 버전 (108줄): 아이콘 변경 + `text-current` 추가
- 모바일 버전 (182줄): 아이콘 변경 + `text-current` 추가

---

## 3. 메뉴명 변경: '사업소식' → '알림/소식'

### 변경된 파일 목록

| 파일 | 라인 | 변경 전 | 변경 후 |
|------|------|---------|---------|
| `components/main/Header.tsx` | 95 | 사업소식 | 알림/소식 |
| `components/main/Header.tsx` | 169 | 사업소식 | 알림/소식 |
| `components/main/BlogSection.tsx` | 58 | 사업 소식 | 알림/소식 |
| `components/main/BlogSection.tsx` | 108 | 사업소식 전체 목록 | 알림/소식 전체 목록 |
| `app/admin/posts/page.tsx` | 158 | 사업소식 게시글 관리 | 알림/소식 게시글 관리 |

---

## 4. 알림/소식 페이지 페이지네이션 추가

**파일**: `components/main/BlogSection.tsx`

### 추가된 기능
- 전체 게시글 10개씩 페이지네이션
- 이전/다음 버튼
- 페이지 번호 버튼 (현재 페이지 강조 표시)

### 추가된 코드

```tsx
// import 추가
import { ChevronLeft, ChevronRight } from "lucide-react"

// 상수 및 상태 추가
const ITEMS_PER_PAGE = 10
const [currentPage, setCurrentPage] = useState(1)

// 페이지네이션 계산
const totalPages = Math.ceil(posts.length / ITEMS_PER_PAGE)
const startIndex = (currentPage - 1) * ITEMS_PER_PAGE
const endIndex = startIndex + ITEMS_PER_PAGE
const paginatedPosts = posts.slice(startIndex, endIndex)
```

### 변경 사항
- PC 테이블: `posts.map` → `paginatedPosts.map`
- 모바일 리스트: `posts.map` → `paginatedPosts.map`
- 번호 표시: `posts.length - startIndex - index` (전체 기준 역순)
- 페이지네이션 UI 추가 (페이지가 2개 이상일 때만 표시)

---

## 수정된 파일 요약

1. `components/main/Header.tsx`
2. `components/main/BlogSection.tsx`
3. `app/admin/posts/page.tsx`
