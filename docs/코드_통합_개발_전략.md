# 프론트엔드 코드 통합 개발 전략

## 1. 현재 상황 분석

### 1.1 두 코드 구성 비교

| 항목 | HTML 랜딩 페이지 | Admin 페이지 |
|------|-----------------|-------------|
| **기술 스택** | 순수 HTML/CSS/JavaScript | Next.js 14 + TypeScript |
| **파일 구조** | 단일 HTML 파일 | 다중 TSX 컴포넌트 |
| **상태 관리** | 전역 변수 (state 객체) | React useState |
| **라우팅** | JavaScript 뷰 전환 | Next.js App Router |
| **데이터 저장** | 하드코딩된 postDB | LocalStorage |
| **스타일링** | 인라인 CSS (1,350줄+) | Tailwind CSS |
| **UI 컴포넌트** | 직접 구현 | shadcn/ui |
| **코드 라인 수** | ~1,350줄 | ~1,200줄 (분산) |
| **접근성 기능** | 고대비, 글자 크기, 수어 | 없음 |
| **외부 연동** | 5개 (Botpress, Walla 등) | 없음 |

### 1.2 핵심 요구사항

```
┌─────────────────────────────────────────────────────────┐
│                    통합 요구사항                          │
├─────────────────────────────────────────────────────────┤
│ 1. HTML 랜딩 페이지 → 메인 페이지 (/)                    │
│ 2. Admin 페이지 → 서브 경로 (/admin)                     │
│ 3. Admin에서 작성한 게시글 → 메인 페이지에 표시           │
│ 4. 하나의 코드베이스에서 통합 운영                        │
└─────────────────────────────────────────────────────────┘
```

---

## 2. 통합 방안 비교

### 2.1 Option A: HTML → Next.js 변환 (권장)

```
현재 구조:
├── 이대목동 랜딩페이지.html (단일 파일)
└── app/admin/ (Next.js)

변환 후 구조:
app/
├── layout.tsx              # 루트 레이아웃
├── page.tsx                # 메인 페이지 (HTML 변환)
├── globals.css             # 통합 스타일
│
├── (main)/                 # 메인 페이지 그룹
│   ├── components/
│   │   ├── A11yBar.tsx     # 접근성 도구
│   │   ├── Header.tsx      # 헤더/GNB
│   │   ├── HeroSection.tsx # 히어로
│   │   ├── StepsSection.tsx # 이용 방법
│   │   ├── FaqSection.tsx  # FAQ
│   │   ├── ContactSection.tsx # 신청하기
│   │   ├── BlogSection.tsx # 사업소식
│   │   └── PostModal.tsx   # 게시글 팝업
│   └── hooks/
│       └── useAccessibility.ts
│
└── admin/                  # Admin 페이지 (기존 유지)
    ├── layout.tsx
    ├── posts/
    └── featured/
```

**장점:**
- ✅ 동일한 프레임워크에서 데이터 공유 용이
- ✅ Next.js App Router로 라우팅 통합
- ✅ 동일한 LocalStorage 접근
- ✅ 향후 SSR/SSG 적용 가능
- ✅ 코드 재사용성 향상
- ✅ TypeScript로 타입 안정성

**단점:**
- ❌ HTML → React 컴포넌트 변환 작업 필요
- ❌ 인라인 CSS → Tailwind 변환 필요
- ❌ 접근성 기능 React로 재구현

**예상 작업량:**
- HTML 구조 → TSX: 중간
- CSS 변환: 높음 (1,350줄+)
- JavaScript → React hooks: 중간
- 총 예상 작업: **중상**

---

### 2.2 Option B: Admin → 순수 HTML/JS 변환

```
현재 구조:
├── 이대목동 랜딩페이지.html
└── app/admin/ (Next.js)

변환 후 구조:
├── index.html              # 메인 페이지
├── admin/
│   ├── index.html          # Admin 로그인
│   ├── posts.html          # 게시글 목록
│   ├── write.html          # 게시글 작성
│   ├── detail.html         # 게시글 상세
│   └── featured.html       # 주요 소식 설정
├── css/
│   └── admin.css
└── js/
    └── admin.js
```

**장점:**
- ✅ 메인 페이지 변경 불필요
- ✅ 단순한 정적 호스팅 가능

**단점:**
- ❌ 리치 텍스트 에디터 재구현 (Tiptap 대체)
- ❌ shadcn/ui 컴포넌트 모두 재구현
- ❌ React 컴포넌트 → 바닐라 JS 변환
- ❌ 상태 관리 복잡도 증가
- ❌ 유지보수 어려움
- ❌ 향후 확장성 제한

**예상 작업량:**
- React → Vanilla JS: 매우 높음
- Tiptap 대체: 매우 높음
- UI 컴포넌트 재구현: 매우 높음
- 총 예상 작업: **매우 높음**

---

### 2.3 Option C: 별도 서버 + API 연동

```
구조:
┌─────────────────┐     ┌─────────────────┐
│  HTML 서버      │     │  Next.js 서버    │
│  (Nginx/정적)   │────▶│  (/admin)        │
│  포트: 80       │     │  포트: 3000      │
└─────────────────┘     └─────────────────┘
         │                      │
         └──────────┬───────────┘
                    ▼
            ┌───────────────┐
            │  API 서버     │
            │  (데이터 공유) │
            └───────────────┘
```

**장점:**
- ✅ 기존 코드 수정 최소화
- ✅ 독립적인 배포 가능

**단점:**
- ❌ 복잡한 인프라 구성
- ❌ CORS 이슈
- ❌ 별도 API 서버 필요
- ❌ 배포/운영 복잡도 증가
- ❌ LocalStorage 공유 불가 (도메인 분리 시)

**예상 작업량:**
- 코드 변환: 낮음
- 인프라 구성: 높음
- API 개발: 중간
- 총 예상 작업: **중상** (운영 복잡도 높음)

---

## 3. 권장안: Option A (HTML → Next.js 변환)

### 3.1 선택 이유

| 평가 기준 | Option A | Option B | Option C |
|-----------|----------|----------|----------|
| 개발 복잡도 | ⭐⭐⭐ | ⭐ | ⭐⭐ |
| 데이터 연동 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| 유지보수성 | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐ |
| 확장성 | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐ |
| 운영 복잡도 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐ |
| **총점** | **22** | **13** | **12** |

### 3.2 핵심 근거

#### A. Admin은 이미 잘 구성되어 있음
```
Admin 페이지 (Next.js)
├── 완성된 CRUD 기능
├── Tiptap 리치 텍스트 에디터
├── shadcn/ui 컴포넌트
├── 이미지/파일 업로드
└── LocalStorage 데이터 관리
```
→ **Admin을 변환하면 이 모든 것을 재구현해야 함**

#### B. HTML 페이지는 주로 UI 렌더링
```
HTML 랜딩 페이지
├── 정적 콘텐츠 표시
├── 간단한 상호작용 (토글, 슬라이더)
├── 외부 서비스 연동 (링크)
└── 접근성 기능
```
→ **React 컴포넌트로 변환이 상대적으로 단순함**

#### C. 데이터 연동 필수
```
Admin (작성) → LocalStorage → 메인 페이지 (표시)

같은 Next.js 앱이면:
- 동일한 localStorage 접근
- 동일한 데이터 타입 공유
- 실시간 업데이트 가능
```

#### D. 향후 확장성
```
현재: LocalStorage
미래: API + Database

Next.js 통합 시:
- API Routes 쉽게 추가
- SSR/SSG로 SEO 최적화
- 서버 컴포넌트 활용
```

---

## 4. 변환 전략 상세

### 4.1 변환 우선순위

```
Phase 1: 기본 구조 (필수)
├── HTML 구조 → TSX 컴포넌트
├── CSS 변수 → Tailwind CSS
└── 라우팅 통합

Phase 2: 핵심 기능 (필수)
├── 접근성 도구 → React Hook
├── 슬라이더 → React 상태
├── 모바일 메뉴 → React 상태
└── 게시글 표시 → LocalStorage 연동

Phase 3: 부가 기능 (선택)
├── FAQ 아코디언 → details/summary 또는 Radix
├── 게시글 팝업 → Dialog 컴포넌트
└── 외부 서비스 연동 유지
```

### 4.2 컴포넌트 분해 계획

```
이대목동 랜딩페이지.html (1,350줄+)
                ↓ 분해
┌──────────────────────────────────────────┐
│ app/(main)/page.tsx                      │
│   └── 메인 페이지 조합                   │
├──────────────────────────────────────────┤
│ components/                               │
│ ├── a11y/                                │
│ │   ├── A11yBar.tsx (100줄)              │
│ │   └── useA11y.ts (50줄)                │
│ ├── layout/                              │
│ │   ├── Header.tsx (80줄)                │
│ │   ├── MobileMenu.tsx (60줄)            │
│ │   └── Footer.tsx (30줄)                │
│ ├── sections/                            │
│ │   ├── HeroSection.tsx (100줄)          │
│ │   ├── StepsSection.tsx (60줄)          │
│ │   ├── FaqSection.tsx (80줄)            │
│ │   └── ContactSection.tsx (50줄)        │
│ ├── blog/                                │
│ │   ├── BlogSection.tsx (100줄)          │
│ │   ├── FeaturedCards.tsx (80줄)         │
│ │   ├── PostTable.tsx (60줄)             │
│ │   └── PostModal.tsx (100줄)            │
│ └── ui/                                  │
│     └── (기존 shadcn/ui 재사용)          │
├──────────────────────────────────────────┤
│ hooks/                                    │
│ ├── useAccessibility.ts                  │
│ ├── useSlider.ts                         │
│ └── usePosts.ts                          │
├──────────────────────────────────────────┤
│ styles/                                   │
│ └── main.css (접근성 스타일)             │
└──────────────────────────────────────────┘
```

### 4.3 CSS 변환 전략

#### 기존 CSS 변수 매핑
```css
/* 기존 HTML */
:root {
    --primary: #004c28;
    --primary-dark: #00381e;
    --secondary: #0056b3;
    --text-black: #1a1a1a;
    --text-gray: #4a4a4a;
    --bg-light: #f5f7f9;
}
```

```typescript
// tailwind.config.ts
const config = {
  theme: {
    extend: {
      colors: {
        'ewha-green': '#004c28',
        'ewha-green-dark': '#00381e',
        'ewha-blue': '#0056b3',
      },
    },
  },
}
```

#### 복잡한 스타일은 CSS 모듈로
```css
/* styles/accessibility.module.css */
.lowVision {
    background-color: #000 !important;
    color: #FFF !important;
    zoom: 1.2;
}

.lowVision * {
    border-color: #00FFFF !important;
    box-shadow: none !important;
}
```

### 4.4 JavaScript → React 변환 예시

#### 기존 코드 (Vanilla JS)
```javascript
// 상태 관리
const state = {
    lowVision: false,
    sign: false,
    fontScaleIndex: 0
};

function toggleLowVision() {
    state.lowVision = !state.lowVision;
    const btn = document.getElementById('btn-lowvision');
    btn.setAttribute('aria-pressed', state.lowVision);

    if (state.lowVision) {
        document.body.classList.add('low-vision');
        btn.classList.add('active');
    } else {
        document.body.classList.remove('low-vision');
        btn.classList.remove('active');
    }
}
```

#### 변환 후 (React Hook)
```typescript
// hooks/useAccessibility.ts
import { useState, useEffect } from 'react'

type FontScale = 100 | 125 | 150

interface AccessibilityState {
  lowVision: boolean
  signLanguage: boolean
  fontScale: FontScale
}

export function useAccessibility() {
  const [state, setState] = useState<AccessibilityState>({
    lowVision: false,
    signLanguage: false,
    fontScale: 100,
  })

  // 고대비 모드 토글
  const toggleLowVision = () => {
    setState(prev => {
      const newLowVision = !prev.lowVision
      return {
        ...prev,
        lowVision: newLowVision,
        fontScale: newLowVision ? 100 : prev.fontScale, // 고대비 시 기본값
      }
    })
  }

  // 수어 영상 토글
  const toggleSignLanguage = () => {
    setState(prev => ({
      ...prev,
      signLanguage: !prev.signLanguage,
    }))
  }

  // 글자 크기 변경
  const changeFontSize = (direction: 1 | -1) => {
    if (state.lowVision) return // 고대비 모드에서 비활성화

    const scales: FontScale[] = [100, 125, 150]
    const currentIndex = scales.indexOf(state.fontScale)
    const newIndex = Math.max(0, Math.min(2, currentIndex + direction))

    setState(prev => ({
      ...prev,
      fontScale: scales[newIndex],
    }))
  }

  // body에 클래스 적용
  useEffect(() => {
    if (state.lowVision) {
      document.body.classList.add('low-vision')
    } else {
      document.body.classList.remove('low-vision')
    }

    document.documentElement.style.fontSize = `${state.fontScale}%`
  }, [state.lowVision, state.fontScale])

  return {
    ...state,
    toggleLowVision,
    toggleSignLanguage,
    changeFontSize,
  }
}
```

#### 컴포넌트에서 사용
```tsx
// components/a11y/A11yBar.tsx
'use client'

import { Eye, Plus, Minus } from 'lucide-react'
import { useAccessibility } from '@/hooks/useAccessibility'

export function A11yBar() {
  const {
    lowVision,
    signLanguage,
    fontScale,
    toggleLowVision,
    toggleSignLanguage,
    changeFontSize,
  } = useAccessibility()

  return (
    <nav className="sticky top-0 z-[2000] bg-[#222] text-white py-2.5 border-b border-[#444]">
      <div className="max-w-[1200px] mx-auto px-5 flex justify-between items-center">
        <div className="font-bold text-sm flex items-center gap-2">
          <i className="fa-solid fa-universal-access" />
          <span className="hidden md:inline">접근성 도구</span>
        </div>

        <div className="flex gap-2.5 items-center">
          {/* 고대비 버튼 */}
          <button
            className={`flex items-center gap-1.5 px-3 py-1.5 rounded text-sm font-medium border transition-all
              ${lowVision
                ? 'bg-white text-black border-white'
                : 'bg-[#444] text-white border-[#666] hover:bg-[#555]'
              }`}
            onClick={toggleLowVision}
            aria-pressed={lowVision}
            aria-label="고대비 모드 켜기/끄기"
          >
            <Eye className="h-4 w-4" />
            <span className="hidden md:inline">고대비</span>
          </button>

          {/* 글자 크기 조절 */}
          <div className="flex items-center bg-[#444] border border-[#666] rounded overflow-hidden">
            <button
              className="px-3 py-1.5 border-r border-[#666] hover:bg-[#555] disabled:opacity-50"
              onClick={() => changeFontSize(-1)}
              disabled={lowVision || fontScale === 100}
              aria-label="글자 크기 축소"
            >
              <Minus className="h-4 w-4" />
            </button>
            <span className="px-2.5 text-xs font-semibold text-[#ddd] hidden md:flex items-center gap-1">
              글자
            </span>
            <button
              className="px-3 py-1.5 hover:bg-[#555] disabled:opacity-50"
              onClick={() => changeFontSize(1)}
              disabled={lowVision || fontScale === 150}
              aria-label="글자 크기 확대"
            >
              <Plus className="h-4 w-4" />
            </button>
          </div>

          {/* 수어 영상 버튼 */}
          <button
            className={`flex items-center gap-1.5 px-3 py-1.5 rounded text-sm font-medium border transition-all
              ${signLanguage
                ? 'bg-white text-black border-white'
                : 'bg-[#444] text-white border-[#666] hover:bg-[#555]'
              }`}
            onClick={toggleSignLanguage}
            aria-pressed={signLanguage}
            aria-label="수어 영상 켜기/끄기"
          >
            <span>🤟</span>
            <span className="hidden md:inline">수어 영상</span>
          </button>
        </div>
      </div>
    </nav>
  )
}
```

### 4.5 데이터 연동 계획

#### 게시글 데이터 공유
```typescript
// lib/posts.ts - Admin과 메인 페이지 공유
export type Post = {
  id: number
  title: string
  content: string
  thumbnailImage?: string
  category: "공지" | "행사" | "뉴스"
  status: boolean
  viewCount: number
  publishedAt: string
  createdAt: string
  updatedAt: string
  attachment?: {
    name: string
    data: string
    size: number
  } | null
}

// 게시글 조회 (메인 페이지용)
export function getPosts(): Post[] {
  if (typeof window === 'undefined') return []
  const stored = localStorage.getItem('news_posts')
  return stored ? JSON.parse(stored) : []
}

// 활성화된 게시글만 조회
export function getActivePosts(): Post[] {
  return getPosts().filter(post => post.status === true)
}

// 주요 소식 슬롯 조회
export function getFeaturedSlots(): { slot1Id: number | null; slot2Id: number | null; slot3Id: number | null } {
  if (typeof window === 'undefined') return { slot1Id: null, slot2Id: null, slot3Id: null }
  const stored = localStorage.getItem('featured_slots')
  return stored ? JSON.parse(stored) : { slot1Id: null, slot2Id: null, slot3Id: null }
}

// 주요 소식 게시글 조회
export function getFeaturedPosts(): Post[] {
  const posts = getActivePosts()
  const slots = getFeaturedSlots()
  const slotIds = [slots.slot1Id, slots.slot2Id, slots.slot3Id].filter(Boolean) as number[]

  return slotIds
    .map(id => posts.find(p => p.id === id))
    .filter((p): p is Post => p !== undefined)
}
```

#### 메인 페이지에서 게시글 표시
```tsx
// components/blog/FeaturedCards.tsx
'use client'

import { useEffect, useState } from 'react'
import { Post, getFeaturedPosts } from '@/lib/posts'
import { Badge } from '@/components/ui/badge'

export function FeaturedCards() {
  const [posts, setPosts] = useState<Post[]>([])

  useEffect(() => {
    setPosts(getFeaturedPosts())
  }, [])

  return (
    <div className="grid grid-cols-1 md:grid-cols-3 gap-6 mb-15">
      {posts.map((post) => (
        <div
          key={post.id}
          className="border rounded-2xl overflow-hidden bg-white cursor-pointer transition-all hover:-translate-y-2 hover:shadow-lg"
        >
          <div className="w-full h-55 bg-gray-100 flex items-center justify-center overflow-hidden">
            {post.thumbnailImage ? (
              <img
                src={post.thumbnailImage}
                alt={post.title}
                className="w-full h-full object-cover"
              />
            ) : (
              <i className="fa-regular fa-image text-4xl text-gray-400" />
            )}
          </div>
          <div className="p-6 flex-1 flex flex-col">
            <Badge className="self-start mb-3">{post.category}</Badge>
            <h3 className="text-xl font-bold mb-2.5 line-clamp-2">{post.title}</h3>
            <p className="text-sm text-gray-500 mt-auto">
              {new Date(post.publishedAt).toLocaleDateString('ko-KR')}
            </p>
          </div>
        </div>
      ))}
    </div>
  )
}
```

---

## 5. 변환 작업 목록

### 5.1 Phase 1: 기본 구조 (1-2일)

| 작업 | 파일 | 우선순위 |
|------|------|----------|
| 프로젝트 구조 재구성 | `app/` 폴더 | 🔴 높음 |
| 루트 레이아웃 설정 | `app/layout.tsx` | 🔴 높음 |
| 메인 페이지 틀 생성 | `app/page.tsx` | 🔴 높음 |
| CSS 변수 Tailwind 통합 | `tailwind.config.ts` | 🔴 높음 |
| 접근성 CSS 분리 | `styles/accessibility.css` | 🟡 중간 |

### 5.2 Phase 2: 핵심 컴포넌트 (2-3일)

| 작업 | 파일 | 우선순위 |
|------|------|----------|
| 접근성 도구 바 | `components/a11y/A11yBar.tsx` | 🔴 높음 |
| 접근성 훅 | `hooks/useAccessibility.ts` | 🔴 높음 |
| 헤더 컴포넌트 | `components/layout/Header.tsx` | 🔴 높음 |
| 모바일 메뉴 | `components/layout/MobileMenu.tsx` | 🔴 높음 |
| 히어로 섹션 | `components/sections/HeroSection.tsx` | 🔴 높음 |
| 슬라이더 훅 | `hooks/useSlider.ts` | 🟡 중간 |

### 5.3 Phase 3: 콘텐츠 섹션 (1-2일)

| 작업 | 파일 | 우선순위 |
|------|------|----------|
| 이용 방법 섹션 | `components/sections/StepsSection.tsx` | 🟡 중간 |
| FAQ 섹션 | `components/sections/FaqSection.tsx` | 🟡 중간 |
| 신청하기 섹션 | `components/sections/ContactSection.tsx` | 🟡 중간 |
| 푸터 | `components/layout/Footer.tsx` | 🟢 낮음 |

### 5.4 Phase 4: 게시글 연동 (2-3일)

| 작업 | 파일 | 우선순위 |
|------|------|----------|
| 게시글 유틸리티 | `lib/posts.ts` | 🔴 높음 |
| 사업소식 섹션 | `components/blog/BlogSection.tsx` | 🔴 높음 |
| 주요 소식 카드 | `components/blog/FeaturedCards.tsx` | 🔴 높음 |
| 게시글 테이블 | `components/blog/PostTable.tsx` | 🟡 중간 |
| 게시글 팝업 | `components/blog/PostModal.tsx` | 🟡 중간 |
| 게시글 훅 | `hooks/usePosts.ts` | 🟡 중간 |

### 5.5 Phase 5: 외부 연동 (0.5-1일)

| 작업 | 파일 | 우선순위 |
|------|------|----------|
| Botpress 스크립트 | `app/layout.tsx` | 🟢 낮음 |
| FontAwesome 설정 | `app/layout.tsx` | 🟢 낮음 |
| 외부 링크 검증 | 전체 | 🟢 낮음 |

---

## 6. 예상 결과물

### 6.1 최종 디렉토리 구조
```
app/
├── layout.tsx              # 루트 레이아웃 (외부 스크립트 포함)
├── page.tsx                # 메인 페이지
├── globals.css             # 전역 스타일
│
└── admin/                  # Admin 페이지 (기존 유지)
    ├── layout.tsx
    ├── page.tsx            # 로그인 (기존 app/page.tsx 이동)
    ├── posts/
    │   ├── page.tsx
    │   ├── write/page.tsx
    │   └── [id]/page.tsx
    └── featured/
        └── page.tsx

components/
├── a11y/
│   └── A11yBar.tsx
├── layout/
│   ├── Header.tsx
│   ├── MobileMenu.tsx
│   └── Footer.tsx
├── sections/
│   ├── HeroSection.tsx
│   ├── SignSection.tsx
│   ├── StepsSection.tsx
│   ├── FaqSection.tsx
│   └── ContactSection.tsx
├── blog/
│   ├── BlogSection.tsx
│   ├── FeaturedCards.tsx
│   ├── PostTable.tsx
│   └── PostModal.tsx
├── ui/                     # 기존 shadcn/ui
└── theme-provider.tsx

hooks/
├── useAccessibility.ts
├── useSlider.ts
├── usePosts.ts
└── use-mobile.ts           # 기존

lib/
├── posts.ts                # 게시글 데이터 유틸리티
└── utils.ts                # 기존

styles/
├── globals.css             # 기존
└── accessibility.css       # 접근성 관련 CSS
```

### 6.2 라우팅 구조
```
/                   → 메인 페이지 (변환된 HTML)
/admin              → Admin 로그인
/admin/posts        → 게시글 목록
/admin/posts/write  → 게시글 작성
/admin/posts/[id]   → 게시글 상세/수정
/admin/featured     → 주요 소식 설정
```

### 6.3 데이터 흐름
```
Admin (/admin/posts/write)
    │
    └─▶ localStorage.setItem('news_posts', [...])
                    │
                    ▼
    메인 페이지 (/) ◀── localStorage.getItem('news_posts')
                    │
                    ├─▶ FeaturedCards (주요 소식 3개)
                    └─▶ PostTable (전체 게시글)
```

---

## 7. 예상 일정

| Phase | 작업 내용 | 예상 기간 |
|-------|----------|----------|
| Phase 1 | 기본 구조 | 1-2일 |
| Phase 2 | 핵심 컴포넌트 | 2-3일 |
| Phase 3 | 콘텐츠 섹션 | 1-2일 |
| Phase 4 | 게시글 연동 | 2-3일 |
| Phase 5 | 외부 연동 | 0.5-1일 |
| 테스트 | 통합 테스트 | 1일 |
| **총계** | | **8-12일** |

---

## 8. 결론

### 8.1 권장 접근 방식
**Option A: HTML 랜딩 페이지를 Next.js로 변환**

### 8.2 핵심 이유
1. **Admin 유지 효율성**: 이미 완성된 CRUD, 에디터, UI 컴포넌트 재사용
2. **데이터 연동 용이**: 동일 앱에서 LocalStorage 공유
3. **유지보수성**: 단일 코드베이스, TypeScript 타입 안정성
4. **확장성**: 향후 백엔드 API, SSR/SSG 적용 용이

### 8.3 주의사항
- 접근성 기능(고대비, 수어)은 꼼꼼하게 테스트 필요
- CSS 변환 시 기존 스타일 동일하게 유지
- 외부 서비스 연동(Botpress, Walla 등) 정상 동작 확인

---

이 문서는 두 프론트엔드 코드 통합을 위한 개발 전략을 분석한 내용입니다.
